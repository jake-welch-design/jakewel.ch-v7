{
  "name": "jakewelch-api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute jakewelch-site --file=./schema.sql --remote",
    "db:init:local": "wrangler d1 execute jakewelch-site --file=./schema.sql --local"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
